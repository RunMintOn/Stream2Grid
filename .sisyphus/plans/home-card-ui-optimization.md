# 首页项目卡片 UI 优化计划 (重新执行)

## 任务目标
优化首页项目卡片的布局，实现大图标右置、项数最右、删除按钮绝对定位。

## 技术方案
1. **重构卡片布局**：使用 `flex justify-between items-center`。
2. **左侧容器**：显示名称、默认标签、时间戳。
3. **右侧容器**：放置收集箱大图标（装饰性，仅 Inbox）和项数徽章。
4. **统一删除按钮**：改为右上角悬浮绝对定位 (`absolute top-0 right-0`)，与内部卡片一致。
5. **视觉优化**：图标尺寸恢复为 `w-10 h-10`，项数徽章固定在最右侧。

## 修改文件
- `src/sidepanel/components/layout/ProjectList.tsx`

## 成功标准
- [x] 收集箱图标不与项数重叠
- [x] 删除按钮改为右上角悬浮，交互逻辑一致
- [x] 项数徽章固定在最右侧
- [x] 中间留空，视觉更开阔
- [x] 构建成功 (`npm run build`)
